{
  "name": "io-eudiw-wpb",
  "private": true,
  "scripts": {
    "code-review": "turbo run typecheck lint:check test:coverage",
    "lint": "turbo run lint",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "build": "turbo run build",
    "version": "changeset version",
    "release": "changeset tag",
    "generate": "turbo run generate"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@pagopa/typescript-config-node": "*",
    "@vitest/coverage-v8": "^2.1.1",
    "prettier": "3.1.1",
    "turbo": "^1.12.5",
    "typescript": "^5.4.5",
    "vitest": "^2.1.1"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "yarn@4.1.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
